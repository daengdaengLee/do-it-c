#include <stdio.h>

int Sum(int value1, int value2) {
  // int result = value1 + value2;
  // return result;
  return value1 + value2;
}

void main() {
  // int s;
  // s = Sum(2, 3);
  // int s = Sum(2, 3);
  // printf("Result of Sum function is %d \n", s);
  printf("Result of Sum function is %d \n", Sum(4, 5));
}
